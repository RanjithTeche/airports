spring:
    profiles:
        active: dev
    application:
        name: airports
    cloud:
        config:
            uri: 'http://localhost:8888'
management:
    endpoints:
        web:
            exposure: {include: '*'}
